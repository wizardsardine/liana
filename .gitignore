# Build artifacts
tags
target/

__pycache__
dummy_config.toml
contrib/coincubed_config_local.toml
regtest/
venv/
pytest.log
TODO
**/target
**/dist
Xcode_12.2.xip
.idea/
fuzz/corpus
result
release_build
release_assets
.env
bin/
lib/
pyvenv.cfg
plans/
.claude/

.vscode
coincube-gui/default
contrib/release/macos/certificates.p12
contrib/release/macos/Coincube.icns
contrib/release/wix/Coincube.ico
contrib/icons
default

# OS
.DS_Store
Thumbs.db

# Sonarqube
.scannerwork/
lcov.info
clippy-report.json
audit-report.json
audit-sonar.json
clippy-sonar.json
wildcard-cleanup-plan.md
# Understand-Anything: keep architecture graph local (public repo)
.understand-anything/

# Bundle assembled at the repo root by the local signing procedure in
# docs/MACOS_KEYCHAIN_ENTITLEMENT.md §4. Build output, never committed.
/Tenshu.app/

# Local build-and-sign convenience script. Deliberately not committed: it signs
# with whatever Developer ID identity the running Mac's login keychain holds,
# and CI stays the source of truth for shipped artifacts.
contrib/release/macos/build-sign-local.sh
# Its outputs, alongside the bundle above.
/tenshu-*.dmg
/tenshu-*-notarize.zip
