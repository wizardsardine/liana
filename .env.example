# Coincube GUI - Environment Variables (example)
# Copy this file to `.env` for local development.

# --- API Keys ---
# Mavapay API key used by the native Africa flow.
MAVAPAY_API_KEY=

# Mavapay webhook secret for HMAC signature verification
# This must match the secret used when registering the webhook with Mavapay
# Generate a secure random string: openssl rand -hex 32
MAVAPAY_WEBHOOK_SECRET=

# Onramper API key used by the embedded webview widget.
# Debug builds: read from .env at runtime.
# Release builds: read at COMPILE TIME via option_env!("ONRAMPER_API_KEY").
#   The build will attempt to read this from your .env automatically in release builds.
#   Alternatively, export it in the build environment, e.g.:
#     ONRAMPER_API_KEY=pk_live_xxx cargo build --release
ONRAMPER_API_KEY=

# Signing secret used to generate and embed signatures to be used with the Onramper Widget
# These are absolutely necessary for some crypto providers to function correctly
ONRAMPER_SIGNING_SECRET=

# --- API Configuration ---
# Coincube API base URL for geolocation and registration services
COINCUBE_API_URL=

# --- Geolocation / Development Overrides ---
# Force geolocation result to bypass ip geolocation, can also be used at runtime
FORCE_ISOCODE=

# Breez SDK API Key (for Lightning Network features)
# Get your API key at: https://sdk-doc-liquid.breez.technology/guide/getting_started.html#api-key
BREEZ_API_KEY=your_test_api_key_here